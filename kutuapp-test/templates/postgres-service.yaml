apiVersion: v1
kind: Service
metadata:
  labels:
    app: kutuapp
    component: postgres
  name: db
  namespace: kutuapp-test
spec:
  type: ClusterIP
  ports:
  - name: "db-service"
    port: 5432
    targetPort: 5432
  selector:
    app: kutuapp
    component: postgres